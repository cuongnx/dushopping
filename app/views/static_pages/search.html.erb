<div class="search-form">
	<fieldset>
		<legend>
			Advanced Search
		</legend>
		<%= render "shared/search_form" %>
	</fieldset>
</div>
<% if @products.empty? %>
<h3>No results match your criteria, please try other options</h3>
<% else %>
<%= render 'shared/product_table' %>
<% end %> 